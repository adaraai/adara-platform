import {
  Inter_400Regular,
  Inter_500Medium,
  Inter_600SemiBold,
  Inter_700Bold,
} from "@expo-google-fonts/inter";
import {
  Outfit_500Medium,
  Outfit_600SemiBold,
  Outfit_700Bold,
} from "@expo-google-fonts/outfit";

/**
 * Outfit carries the rounded geometric voice for headlines; Inter handles
 * everything that has to stay readable at 13px. The keys must match the
 * family names in `tailwind.config.js`.
 */
export const fontMap = {
  Outfit_500Medium,
  Outfit_600SemiBold,
  Outfit_700Bold,
  Inter_400Regular,
  Inter_500Medium,
  Inter_600SemiBold,
  Inter_700Bold,
};
